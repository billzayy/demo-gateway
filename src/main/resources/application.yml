server:
  port: 3634

spring:
  datasource:
    url: jdbc:postgresql://34.126.164.82:5432/student
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true