server:
  port: 3634

spring:
  datasource:
    url: jdbc:postgresql://34.142.141.98:5432/postgres
    username: root
    password: password
  jpa:
    hibernate:
#      ddl-auto: create-drop
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true